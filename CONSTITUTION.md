# Qlo 前端專案憲法（Constitution）

> 這份文件是本專案的最高規則。所有畫面與元件的製作都必須遵守。
> 當「使用者當下的指示」與「憲法」衝突時，以憲法為準，並先向使用者確認。

---

## 一、四大原則

1. **邊做邊寫（Living Documentation）**
   隨著建立過程，逐步累積 `DESIGN-SYSTEM.md`。這份文件的目標是「之後交給其他專案也能直接使用」。

2. **嚴格遵循（Strict Composition）**
   畫面只能用 Design System 中「已確定（正式區）」的元件來組裝。
   若畫面需要一個正式區還沒有的元件 → 先停下來，走「元件補齊流程」。

3. **不猜測、需同意（No Guessing, Consent Required）**
   - 遇到任何不確定的事，一定先問使用者。可以提供建議做法，但不可自行猜測後直接實作定案。
   - **任何檔案要進入正式 Design System 元件庫（`src/components/`），一定要取得使用者明確同意。**

4. **留下紀錄（Traceable Changes）**
   每一次「確定的更新」，都要用白話寫進 `CHANGELOG.md`。

---

## 二、技術決定（已拍板）

| 項目 | 決定 |
|------|------|
| 框架 | React + TypeScript |
| 樣式 | 原生 CSS Variables（Design Token 的底層） |
| 預覽 | 先做輕量預覽；結構對齊 Storybook CSF，之後可平滑升級 |
| 目標平台 | 網頁（Web）優先 |

---

## 三、資料夾分區

| 資料夾 | 狀態 | 說明 |
|--------|------|------|
| `src/tokens/` | 🎨 | Design Token → CSS Variables（使用者提供，Claude 落地） |
| `src/_draft/` | 🚧 草稿區 | 製作中、等待使用者確認的元件 |
| `src/components/` | ✅ 正式區 | 已確定的元件。**只有經使用者同意才能搬進來** |
| `src/screens/` | 🖼️ | 用「正式區元件」組裝的畫面 |
| `preview/` | 👀 | 輕量互動預覽（未來轉 Storybook） |

**鐵則：草稿區與正式區實體分開。元件從 `_draft/` → `components/` 的搬移，等同「轉正式」，必須經使用者同意。**

---

## 四、標準流程（畫面 / 元件通用）

1. **使用者提供** Idea 或畫面
2. **Claude 製作**（在 `_draft/`）：依使用者說法 + 本憲法 + 既有規則
3. **Claude 提供互動預覽**：可切換 variant / state；主動指出缺漏的狀態，提出建議並詢問
4. **使用者確認無誤** → 才「轉正式」：
   - 搬移 `_draft/` → `components/`
   - 更新 `DESIGN-SYSTEM.md`
   - 更新 `CHANGELOG.md`

> **測試畫面（Draft 階段）**：允許用**草稿區元件**（也可含正式區元件）組「測試畫面」一起評估。
> 畫面上要調整時依 **6.6** 分流：草稿元件直接改、正式元件則提新變體走轉正式流程。元件轉正式後畫面自動沿用。

---

## 五、使用者的輸入方式

- **Design Token**：使用者直接提供，Claude 負責落地成 CSS Variables。
- **目標畫面**：使用者提供最終想呈現的樣子。
- **缺的元件**：畫面用到、但正式區還沒有的元件，雙方一個一個補齊。

---

## 六、工程實作規範（Engineering Rules）

> 由使用者（設計師）提出方向，Claude（工程）翻成精確規則並補齊盲區。
> 這些規則和「不猜測、需同意」同等重要，寫元件與畫面時一律遵守。

### 6.1 不寫死值（No Hardcoded Values）
- 顏色、間距、圓角、字級、陰影等**一律引用 Design Token**（CSS Variables），不得直接寫死數字或色碼。
- 若某個值目前**沒有對應 token**：不可自行寫死，改為
  1) 用元件層級變數（`--qlo-元件-屬性`）暫定並註解，且
  2) 記進 `FIGMA-SYNC.md` 請設計端補 token。
- **允許的例外**（結構性、非設計語彙）：預設尺寸、`min-width` / `max-width` / `min-height` 限制、`z-index` 層級、動畫時間等；這些仍盡量集中管理、不散落。

### 6.2 單位一律用 rem，不用 px
- 字級、間距、圓角、元件尺寸**用 `rem`**（相對根字級，使用者放大字體時能等比縮放，較無障礙）。
- 換算基準：**1rem = 16px**（Figma 的 px ÷ 16 = rem）。例：24px→1.5rem、16px→1rem、12px→0.75rem、8px→0.5rem。
- **line-height 用無單位**（如 `1.5`），不要加 px/rem。
- **版面寬高**優先用相對／彈性單位：`%`、`fr`、`minmax()`、`ch`（文字寬度）、`vw/vh`（謹慎），而非固定 px。
- **落地方式（已定案）**：撰寫時照 Figma 用 px（`tokens.css` 維持 Figma 匯出原樣、不手改），
  **建置時由 PostCSS（`postcss-pxtorem`）自動把 px 轉成 rem** 輸出（含細邊框）。設定見 `postcss.config.js`。
  → 結論：原始碼可有 px，但**瀏覽器最終收到的一律是 rem**。

### 6.3 尺寸與寬度
- 元件可有**預設尺寸**，但要能被內容撐開／縮小；不要用固定死寬高把內容裁掉。
- 用 `min-width` 防止塌陷、`max-width` 防止爆版；容器內容過長時**該容器自己捲動**（`overflow` 處理），不要把整個版面撐破。

### 6.4 文字與極端值（Text & Edge Cases）
凡是**會顯示文字**或**有文字輸入**的地方，都要處理極端值。每個這類元件的 Storybook 都要放「極端值 story」：
- **過長文字**：明確選擇策略——單行截斷 `…`（`text-overflow: ellipsis`）、多行截斷（`line-clamp`）、或自動換行；並在 story 放超長字驗證。
- **無空白長字串**（如網址、檔名）：`overflow-wrap: anywhere` / `word-break`，避免撐破容器。
- **空值 / 只有空白**：要有合理呈現（placeholder、隱藏、預設值），不可破版或報錯。
- **數字極端值**：很大的數字、負數、小數、千分位格式化。
- **多語系擴張**：同一句話換語言可能變長（歐語常 +30%）或字型不同（中日韓）；版面要容得下，不能假設英文長度。
- **min/max**：輸入類元件設 `min-width` 防塌陷、`max-width` 或截斷防爆版；必要時限制輸入長度（`maxlength`）。

### 6.5 Claude（工程）幫忙補的盲區
- **無障礙 a11y**：互動元件要有 `:focus-visible` 樣式、可鍵盤操作；文字與背景對比足夠；可點擊目標**至少 44px（2.75rem）**。
- **狀態要齊全**：互動元件至少涵蓋 default / hover / focus / active(pressed) / disabled；視情況加 loading / error / empty，缺的主動提出問使用者。
- **內容用 props 傳入**：文案、數字、圖片路徑不寫死在元件內；元件只管樣式與結構。
- **媒體**：圖片 `max-width: 100%`，不溢出容器。
- **彈性版面**：不假設固定視窗寬（桌面版固定視窗尺寸的規則另訂，見專案討論）。

### 6.6 改在源頭層，保持引用鏈（Single Source of Truth）
前提：**page 只負責組裝與傳 props**，不承載元件樣式；值一律走 token。在畫面上發現要調整時，依「該元件在哪一區」分流：

- **A. 畫面引用的是草稿區元件（`_draft/`，尚未轉正式）**
  - 直接改**元件本身**，讓所有用到它的畫面一起生效；值不對就改 **token／`--qlo-*`**，不在 page 寫死。
- **B. 畫面引用的是正式區元件（`components/`，已定案）**
  - 要調整 ＝ 代表**正式元件不夠用**。**不可直接改正式元件**（它已確定、其他畫面靠它，隨手改會波及）。
  - Claude 要**停下來問使用者**：是否**新增一個變體**（或新元件）？不自行動手。
  - 確認要做 → 走既有轉正式流程：在**草稿區**做出提案 → 給使用者看 → 討論確定 → 才補進正式區。

判準：修正若「在 page 改完就沒事」但別的畫面用到同元件卻沒跟著變 → 就是**改錯層**。
好處：單一真相來源；`page → 元件 → token` 一層一層引用不斷鏈，元件轉正式後畫面自動沿用。

## 七、精簡工作法（Efficiency Mode，預設）

> 目的：降低 token 消耗。核心是「少來回、抓代表值、用預覽目視確認取代逐格抓」。

1. Figma 只抓最少必要：一張總圖 + 一次 get_variable_defs + metadata + 每軸 1–2 個代表格的 get_design_context；其餘變體照規律推，不逐格抓。
2. 推論要透明：推出的值與不確定處，列在該元件的 *.figma-spec.md 與回報裡，標「推斷／待確認」，在預覽讓使用者目視確認；有疑慮才追加抓 Figma。（對「絕不猜測」的務實放寬：從規律推＋明列假設＋預覽確認。）
3. 驗證一次到位：信任 Figma 值，用一次 JS 撈齊所有值比對，不截圖+放大+讀頁面多趟。
4. 批次處理：一次給多個相關 node，一趟抓完一起建。
5. 分段開新對話：每 1–2 個元件可開新 session，狀態靠 repo 檔承接。
6. 回報精簡：重點條列，不長篇複述。
7. **驗證分工：肉眼歸使用者、精確值歸 Claude。**
   - **使用者（設計師）目視**：互動會不會動、外觀順不順、比例／破版——靠肉眼與品味，在預覽（localhost 網址）自己看。
   - **Claude（工程）比數值**：間距／圓角／顏色是否精確對 Figma，用「讀原始碼有沒有引用對 token」或一行 `getComputedStyle` 讀 computed 值跟 Figma 數字比對——精確、便宜，不靠截圖。
   - 「精確對 Figma」不是肉眼的活：人眼分不出 16 vs 15px、截圖也受縮放／DPI 失真。且依 6.1 一律引用 token，只要 token 值＝Figma 值，間距就是對的——屬「一開始做對」，非事後量。
8. **開瀏覽器（截圖／實際驅動）只在必要時做，預設不做。**
   - 預設由使用者目視確認；Claude **只在使用者要求截圖存證、或使用者無法自行查看時**才開瀏覽器。
   - 驗過一次就信任，不每次重跑；純靜態／視覺改動不必開瀏覽器。
   - 受控／有狀態元件在預覽**預設就是活的**（框架已自動把 `on<Prop>Change` 接回 args），這類「點了沒反應」已結構性解決，不需專門去驗。
   - 瀏覽器環境不可用時（如 policy 卡住），改用 `tsc` ＋讀原始碼 token ＋邏輯推演並如實說明，**不反覆重試燒 token**。
